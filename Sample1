import pandas as pd

df = pd.read_csv('input_file.csv')
column_to_check = 'column_name'
df['New_Column'] = df[column_to_check].apply(lambda x: 'Yes' if str(x).startswith('aaaa') else 'No')
df.to_csv('output_file.csv', index=False)

print("CSV file has been processed and saved.")
