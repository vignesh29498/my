Scheduled a connect with TCS internal AI team and modified the input document and prompt based on their suggestion.

Achieved around 70% accuracy for unique field handoff files, but still facing issues with repeating fields.

Got another suggestion from TCS internal AI team to reference a sample JSON object for output, but accuracy is still below expectations.

Scheduled a connect with the TCS COE team for further steps.

Successfully generated test scenarios for AI test case generation but couldn't view all due to LLM limitations.

Split the input into smaller parts in a loop to generate test cases in JSON format.

Converted test cases into BDD format and then into Selenium Java code.

Completed a review with the functional team, and generated new test cases and scenarios based on their comments.

Both files were shared with the functional team for final review.
